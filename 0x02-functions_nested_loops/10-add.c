#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @a: first number
 * @b: second number
 *
 * Return: a + b
 */

int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	return (sum);
}
